export enum CurrentAlgorithmEnum {
    merge = 'MergeSort',
    quick = 'QuickSort',
    heap = 'HeapSort',
    bubble = 'BubbleSort'
}

export enum Colors {
    blue = 'var(--blue)',
    green = 'var(--green)',
    red='var(--red)',
    purple= 'var(--purple)',
    yellow = 'var(--yellow)',
}
export type TableElementProps = {
    height: string;
    width: string;
    color: string;
}

